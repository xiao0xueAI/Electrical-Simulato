# 接线系统专家评审（软件工程 + 专业电工双视角）

> 评审对象：`脚本/布线.js`（交互+绘制）、`脚本/事件.js`（触发）、`脚本/引擎.js`（连接图/电流求解）、`数据/布线样式.js`（配置）
> 评审时间：2026-08-01
> 结论：接线的**正交交互、T 型并联分支、电流方向求解**整体是正确且健壮的；主要可优化点在 **①线色不符合中国/IEC 国标、②线型自动推断忽略电源类型、③缺少电源类型/短路/反接的安全告警、④电流大小不可见**，以及若干软件可维护性/性能项。

---

## 一、专业电工视角

### 🔴 P0｜线色不符合中国/IEC 60446 国标（教学正确性）
当前配色（`数据/布线样式.js`）：
- `live` = 红 `#e53935`、 `neutral` = 蓝 `#1e88e5`、 `ground` = 绿 `#43a047`
- `dc_pos` = 橙、 `dc_neg` = 青

问题：红/绿/蓝是**旧英标（BS 7671 已废弃）**配色。中国/ IEC 现行标准：
- **L 火线 = 棕色**
- **N 零线 = 淡蓝色**
- **PE 地线 = 黄绿双色**
- 直流：正极红、负极黑/深灰（蓝已被零线占用，不宜复用）

一个面向中国电工的仿真工具，用红色当火线、绿色当地线，**会教错颜色记忆**，属于方向性错误。
**优化**：`live→#8B5A2B(棕)`、`neutral→#2E86DE(蓝)`、`ground→#9ACD32(黄绿)`；`dc_neg→#37474F(深灰)`。地线若能画黄绿条纹更佳（进阶）。

### 🔴 P0｜线型自动推断忽略电源类型（根因级正确性）
`detectWireType()`（52 行）只看**引脚标签文字**（`+`/`N`/`L`/`GND`…），完全不看该引脚**属于哪个元件、是 AC 还是 DC 源**。
后果举例：
- 电池 `+`（标签 `+`）→ 某引脚标签 `L` 的元件：被识别成 `dc_pos` 接 `live`，语义自相矛盾（电池是 DC，不应产生“live/火线”语义）。
- AC 源 `L` 接到电池 `+` 同样会乱。
**优化**：`resolveWireType()` 增加“端点所属元件类型”入参——若一端是 `ac_source` 则其 `L`/`N` 强制按交流语义；若一端是 `battery*`/`dc_dc` 则其 `+`/`-` 强制按直流语义。这样线色/线型与真实电源严格一致。

### 🟠 P1｜缺少“电源类型不匹配”告警（教学价值高）
`findAllPaths()` 不校验电源与负载的电气类型。现实里把 **220V 交流接到直流电机**、或 **DC 源接到只吃 AC 的负载**是严重错误，但仿真会“照常工作”无提示。
引擎已有 `getPowerType()`（305 行，返回 `ac`/`dc`/`both`），但只在继电器预解算用过。
**优化**：solve 后对每条有效路径做“源类型 vs 载类型”一致性检查：`ac` 源驱动 `dc`-only 负载（motor_dc/led/diode）→ 弹 `warn` 级告警“交流源驱动了直流负载”。

### 🟠 P1｜缺少“短路/反接”检测（安全性）
把电源 `+` 与 `−` 用一根线直连（中间仅开关）时，路径总阻 ≈ 源内阻 `0.1Ω` + 接触 `0.05Ω` ≈ `0.15Ω`，算出 `I = 220/0.15 ≈ 1466A`，`comp.simCurrent` 显示一个离谱大数、箭头速度被 `min(,5)` 封顶但电流数值失真。
`drawWires` 已支持 `w._fault` 红色高亮（666 行），但 `solve()` 当前**不会主动置 `_fault`**。
**优化**：路径总阻 `< 某个阈值`（如 `< 1Ω` 且无熔断器/断路器在路径上）时，置 `w._fault=true` 并在故障面板提示“疑似短路，电流过大”。

### 🟡 P2｜电流大小不可见（教学表现力）
`drawCurrentFlow` 里 `speed = Math.min(|current|/4, 5)`（949 行）：典型负载电流几百 mA，几乎全部被**封顶到 5**，于是粗线/细线、大电流/小电流跑得一样快。
电工希望“电流越大、箭头越快越密”。此前你反馈“太快”，所以正确做法是**保留较慢的整体节奏，但让速度与电流成可见比例**（对数或分段），并让箭头间距随电流变化。

### 🟡 P2｜PE 地线无“共点/接地符号”语义
当前 PE 在仿真里是普通节点（逻辑上全网并联正确），但视觉上接地端子没有“接地符号（⏚）”提示，不够专业。属锦上添花。

---

## 二、软件工程视角

### 🟠 P1｜对齐吸附逻辑重复（可维护性）
“按自由轴吸附到最近引脚”的逻辑在 **两处独立实现且略有差异**：
- `routeClick()`（226–244 行）：用原始 `canvasPos` 做距离比较。
- `drawTempWire()`（763–794 行）：用 `screenToCanvas` 后的 `m` 做比较。
以后改吸附规则（如增加角度吸附）必然漏改一处。
**优化**：抽成单一函数 `WireRouter._alignSnap(head, mouse, snapTarget)`，两处复用。

### 🟠 P1｜每帧 O(N) 扫描全部引脚做参考线（性能）
`drawTempWire()` 每次 `mousemove` 都遍历 `S.components` 的全部引脚算最近距离（767–784 行）。元件多的电路拖线会卡。
**优化**：在 `start()` 时缓存“可吸附引脚列表”（接线期间元件不动），移动时只做投影比较；列表随元件增删刷新。

### 🟡 P2｜阈值常量分散（一致性）
- `findWirePointAt` 默认 18（159 行），但调用处全传 22（事件 133、布线 753）。
- `findWireAt` 默认 12（删除命中）。
- `complete()` 判断“是否需要自动拐角”用 `> S.grid`。
**优化**：把吸附阈值、删除阈值、最小拐角栅格倍数提升为 `WireStyle` 或模块顶部常量，避免“删除比接线难选”的体验差。

### 🟡 P2｜退化导线未防护（健壮性）
`complete()` Pin→Pin 只查 `exists`，不查 `p1≈p2`。在端子附近误点两次可能产生 ~0 长度导线：它仍进 `S.wires`、可被选中/删除，造成脏数据（虽因 `_calcPathLength<30` 不画电流）。
**优化**：`if (dist(head,p2) < S.grid) { UI.toast('距离过短'); return; }`。

### 🟡 P2｜魔法数字/可读性
箭头张角 `2.4` 弧度（~137°）、`shadowBlur 8/6`、`flowColor` 参数名易与“电流色”混淆——建议提升为配置或改名。

---

## 三、优先级与建议实施顺序

| 优先级 | 项 | 类型 | 风险 | 建议 |
|---|---|---|---|---|
| P0 | 线色改 IEC 国标（棕/淡蓝/黄绿） | 视觉/电工 | 中（改变你认可的配色） | 建议做，可一键回退 |
| P0 | 线型推断结合电源类型 | 正确性 | 中（影响旧电路线色） | 建议做，旧电路重算线色更准 |
| P1 | 电源类型不匹配告警 | 功能/教学 | 低 | 建议做 |
| P1 | 短路/反接故障标红 | 安全 | 低 | 建议做 |
| P1 | 吸附逻辑去重 + 引脚缓存 | 性能/可维护 | 中（重构） | 建议做，需回归测试 |
| P2 | 电流大小可见（速度比例） | 视觉/教学 | 中 | 建议做，整体调慢 |
| P2 | 阈值集中/退化导线防护 | 健壮性 | 低 | 建议做 |
| P2 | 接地符号/PE 语义 | 视觉 | 低 | 可选 |

> 说明：P0 两项会改变你目前认可的视觉配色/线型，属于“方向性正确但需你拍板”的改动——我没有擅自改。其余 P1/P2 多为加功能或加防护，不影响你认可的动画观感。

---

## 四、其中已确认“做得对”的地方（无需动）
- 正交预览（上一轮已修）与落线拐角算法一致 ✅
- T 型并联分支的电流累加（父线承载主路+支路电流）物理正确 ✅
- `_flowDir` 由 DFS 路径顺序推导，并联分支方向正确 ✅
- 去重（superset 检测）避免并行路径被重复计入导致电流虚高 ✅
- 继电器触点 COM-NO/NC 按 energized 动态连边 ✅
